# Claude Code local/session settings — machine-specific, not for sharing
.claude/settings.local.json

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
*.swp
*.swo

# Packaged extension artifacts
*.zip
*.crx
*.pem

# Local reference copies of a live TraderPRO page (design/context reference
# only, not for redistribution — may contain the contributor's own session/
# strategy data). Ask the contributor for a fresh saved copy if you need one
# for manual testing or store-screenshot generation; don't assume one exists.
TraderPRO - example page.html
TraderPRO - example page_files/
TraderPRO - etf_strategy (rebalancing).html
TraderPRO - safe_page.html
TraderPRO - safe_page_files/
